{
  "name": "task-incident-tracker",
  "version": "1.0.0",
  "description": "Full-stack Task and Incident Tracking Application",
  "scripts": {
    "prebuild": "cd client && npm install && npm run build",
    "start": "npm run prebuild && docker compose up -d --build",
    "dev": "docker compose up --build",
    "stop": "docker compose down",
    "logs": "docker compose logs -f",
    "clean": "docker compose down -v --remove-orphans && docker system prune -f"
  },
  "keywords": [
    "task-management",
    "incident-tracking", 
    "react",
    "nodejs",
    "express",
    "mongodb",
    "docker"
  ],
  "author": "Task Tracker Team",
  "license": "MIT"
}
